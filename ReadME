/f1-strategy-bot
├── index.html
├── styles.css
├── app.js         → Main entry point / event listeners
├── strategy.js    → simulateStrategy() as main orchestrator
├── charts.js      → drawTireChart() + drawPitWindowsChart()
├── summary.js     → updateRaceSummary()
